import { User, GuildMember } from "discord.js";
import { State } from "../../state/botstate";

export function GetPlayer(member: GuildMember) {
    //let crimeDictionary = State.GetDataStore().GetGuildMemberValue(member, );
}
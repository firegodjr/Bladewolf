{
    "type": "node",
    "request": "launch",
    "name": "Launch Program",
    "program": "${workspaceFolder}/scripts/bot.ts",
    "cwd": "${fileDirname}",
    "outFiles": ["${fileDirname}/**/*.js"]
}